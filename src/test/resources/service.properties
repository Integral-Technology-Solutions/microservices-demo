# This file is where all service specific properties are to be placed
# Note that these properties are only available when running locally using Jetty
# When running on a server, these properties will need to be replicated in the properties file on the server

service.contextPath=/rest/v1